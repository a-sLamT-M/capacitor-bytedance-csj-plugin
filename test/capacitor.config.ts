import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'test.bytedance.csj',
  appName: 'test.bytedance.csj',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
